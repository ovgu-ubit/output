export interface FilterOptions {
    corresponding?: boolean,
    instituteId?: number,
    publisherId?: number,
    contractId?: number,
    pubTypeId?: number,
    oaCatId?: number
}

export interface HighlightOptions {
    corresponding?: boolean,
}